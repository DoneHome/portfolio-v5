# 期权持仓展示模块优化方案

## 1. 项目概述

### 1.1 背景与现状
当前投资记录网站的期权持仓展示功能较为简陋，存在以下问题：
- 方向表达不直观（卖出开仓显示为 `-1张`）
- 风险信息缺失（无价差百分比、虚实值状态）
- 时间维度缺失（无距到期天数）
- 盈亏计算笼统（无法区分权利金收入和浮动盈亏）

### 1.2 目标
设计并实现一个专业的期权持仓展示模块，为卖出看跌期权策略提供关键决策信息。

## 2. 现有数据库结构

### 2.1 `positions` 表结构
```javascript
{
  "symbol": "MU",                    // 标的代码
  "type": "option",                  // 持仓类型：option
  "quantity": -1,                    // 合约数（负数为卖出开仓）
  "cost_price": 2.5,                 // 开仓权利金（每股）
  "option_details": {                // 期权详细信息
    "option_symbol": "MU260424P358", // 完整期权代码
    "strike_price": 358,             // 行权价
    "expiration_date": "2026-04-24", // 到期日
    "option_type": "put",            // 期权类型：call/put
    "transaction_type": "sell"       // 交易类型：buy/sell
  }
}
```

### 2.2 前端处理逻辑
在 `portfolio-api.js` 中，前端将 `option_details.option_symbol` 提取到顶层：
```javascript
if (dbPosition.type === 'option' && dbPosition.option_details && dbPosition.option_details.option_symbol) {
    dbPosition.option_symbol = dbPosition.option_details.option_symbol;
}
```

## 3. 表格字段设计

| 列名 | 计算方式 / 说明 | 数据来源 |
|------|----------------|----------|
| 标的 | 正股代码（如 MU） | `position.symbol` |
| 期权代码 | 完整期权代码（如 MU260424P358） | `position.option_details.option_symbol` |
| 方向 | 买入看涨 / 卖出看涨 / 买入看跌 / 卖出看跌 | `option_type` + `transaction_type` |
| 合约数 | 正整数，如 `1张` | `Math.abs(position.quantity)` |
| 行权价 | 期权行权价格 | `position.option_details.strike_price` |
| 开仓权利金 | **符号约定**：`cost_price` 总是正数<br>- 卖出：收入权利金，`cost_price` 为正<br>- 买入：支出权利金，`cost_price` 为正<br>方向由 `transaction_type` 字段决定 | `position.cost_price * Math.abs(quantity) * 合约乘数` |
| 当前权利金 | 从数据源获取的最新价 | yfinance API |
| 当前股价 | 正股最新价 | yfinance API |
| 价差 (%) | `(当前股价 - 行权价) / 当前股价 * 100` | 实时计算 |
| 虚实状态 | 根据价差判断：虚值 / 平值 / 实值 | 实时计算 |
| 距到期天数 | `到期日 - 当前日期` | `position.option_details.expiration_date` |
| 盈亏平衡点 | 动态计算：<br>- 看跌期权：`行权价 - 开仓权利金`<br>- 看涨期权：`行权价 + 开仓权利金` | 实时计算 |
| 浮动盈亏 | 动态计算：<br>- 卖方：`(开仓权利金 - 当前权利金) × 合约乘数 × 合约数`<br>- 买方：`(当前权利金 - 开仓权利金) × 合约乘数 × 合约数` | 实时计算 |
| 市值 | `当前权利金 × 合约乘数 × 合约数` | 实时计算 |
| 合约乘数 | 数据库中已有 `contract_multiplier` 字段<br>- 默认值：100<br>- 特殊标的：根据实际设置<br>- 优先使用数据库中的值 | 从 `position.option_details.contract_multiplier` 获取 |

## 4. 视觉状态标识

### 4.1 安全边际颜色方案（核心）
**重要**：颜色标识必须基于**安全边际（价差百分比）**，而不是单纯的实值/虚值判断。

#### 4.1.1 卖出看跌期权颜色规则
| 安全边际（价差%） | 颜色 | 状态 | 说明 |
|------------------|------|------|------|
| > 10% | 🟢 绿色 | 安全 | 深度虚值，安全缓冲充足 |
| 5%-10% | 🔵 蓝色 | 关注 | 中度虚值，需要关注 |
| 0%-5% | 🟡 黄色 | 警示 | 浅度虚值，风险较高 |
| ≤ 0% (实值) | 🔴 红色 | 危险 | 已进入实值，立即处理 |

#### 4.1.2 计算公式
- **看跌期权安全边际**：`(当前股价 - 行权价) / 当前股价 × 100%`
- **看涨期权安全边际**：`(行权价 - 当前股价) / 当前股价 × 100%`

#### 4.1.3 实施原则
1. **颜色驱动**：表格颜色严格基于安全边际百分比
2. **文字辅助**："虚实状态"列显示"实值/虚值"文字
3. **风险优先**：实值一律红色，无论价差大小
4. **策略适配**：针对卖出看跌期权优化

### 4.2 盈亏颜色方案
- **绿色**：浮动盈亏为正
- **红色**：浮动盈亏为负
- **灰色**：浮动盈亏为零

## 5. 技术架构

### 5.1 前端架构
```
portfolio-v5/
├── js/
│   ├── options-utils.js      # 期权数据提取和计算工具
│   ├── options-renderer.js   # 期权表格渲染器
│   └── main.js              # 主程序集成
├── index.html               # 添加期权表格HTML
└── css/                     # 样式文件
```

### 5.2 后端架构
```
api/portfolio/
├── options.py              # 期权API路由
├── portfolio.py           # 现有持仓API
└── __init__.py
```

## 6. 核心代码实现

### 6.1 前端数据提取工具（options-utils.js）
```javascript
class OptionsUtils {
    // 从 position 对象提取期权信息
    static extractOptionData(position) {
        if (position.type !== 'option' || !position.option_details) {
            return null;
        }
        
        const details = position.option_details;
        return {
            symbol: position.symbol,
            option_symbol: details.option_symbol,
            quantity: position.quantity,
            cost_price: position.cost_price,
            strike_price: details.strike_price,
            expiration_date: details.expiration_date,
            option_type: details.option_type,
            transaction_type: details.transaction_type
        };
    }
    
    // 计算展示字段
    static calculateDisplayFields(optionData, stockPrice, optionPrice) {
        // 详细计算逻辑...
        return {
            标的: optionData.symbol,
            期权代码: optionData.option_symbol,
            方向: direction,
            合约数: `${quantity}张`,
            行权价: strikePrice.toFixed(2),
            开仓权利金: (costPrice * quantity * 100).toFixed(2),
            当前权利金: currentPremium.toFixed(2),
            当前股价: currentPrice.toFixed(2),
            价差百分比: priceDiffPercent.toFixed(2),
            虚实状态: moneyness,
            距到期天数: daysToExpiry,
            盈亏平衡点: breakEven ? breakEven.toFixed(2) : '-',
            浮动盈亏: totalFloatingPnl.toFixed(2),
            市值: marketValue.toFixed(2),
            状态颜色: statusColor,
            盈亏颜色: pnlColor
        };
    }
}
```

### 6.2 后端API接口（options.py）
```python
@router.get("/batch-quotes")
async def get_batch_option_quotes(option_symbols: str, stock_symbols: str):
    """
    批量获取期权和股票价格
    """
    # 使用 yfinance 获取数据
    pass

@router.get("/position-details")
async def get_option_position_details():
    """
    获取期权持仓的详细展示数据
    """
    # 1. 从数据库获取期权持仓
    # 2. 批量查询价格
    # 3. 计算展示字段
    pass
```

### 6.3 前端渲染器（options-renderer.js）
```javascript
class OptionsRenderer {
    constructor() {
        // 与主页面保持一致：5分钟刷新间隔
        this.refreshInterval = 300000; // 300秒 = 5分钟
        this.refreshTimer = null;
        this.lastUpdate = null;
        this.apiBaseUrl = '/api/portfolio/options';
        this.isRefreshing = false;
    }
    
    async init() {
        // 绑定刷新按钮事件
        document.getElementById('refresh-options')?.addEventListener('click', () => {
            this.triggerManualRefresh();
        });
        
        // 初始加载
        await this.refreshOptionsData();
        
        // 启动定时刷新
        this.startAutoRefresh();
    }
    
    // 刷新期权数据
    async refreshOptionsData(force = false) {
        if (this.isRefreshing && !force) return;
        
        this.isRefreshing = true;
        try {
            const response = await fetch(`${this.apiBaseUrl}/position-details`);
            const result = await response.json();
            
            if (result.success) {
                this.renderOptionsTable(result.data);
                this.updateSummary(result.data);
                this.updateLastUpdated();
            }
        } catch (error) {
            console.error('期权数据刷新失败:', error);
        } finally {
            this.isRefreshing = false;
        }
    }
    
    // 手动刷新触发
    triggerManualRefresh() {
        this.refreshOptionsData(true);
    }
    
    // 启动自动刷新
    startAutoRefresh() {
        if (this.refreshTimer) {
            clearInterval(this.refreshTimer);
        }
        
        this.refreshTimer = setInterval(() => {
            this.refreshOptionsData();
        }, this.refreshInterval);
    }
    
    renderOptionsTable(optionsData) {
        // 渲染表格
    }
    
    createOptionRow(option) {
        // 创建表格行
    }
}
```

### 6.4 与主页面集成
```javascript
// portfolio-v5/js/main.js
class PortfolioApp {
    constructor() {
        this.refreshInterval = 300000; // 5分钟
        this.refreshTimer = null;
        this.isRefreshing = false;
        this.lastData = null;
        this.db = null;
        this.backupService = null;
        this.currentMarketFilter = 'all';
        
        // 添加期权渲染器实例
        this.optionsRenderer = null;
        
        this.init();
    }
    
    async init() {
        // ... 现有初始化代码
        
        // 初始化期权渲染器
        await this.initOptionsRenderer();
        
        // ... 其他初始化
    }
    
    async initOptionsRenderer() {
        try {
            // 创建期权渲染器实例
            this.optionsRenderer = new OptionsRenderer();
            await this.optionsRenderer.init();
        } catch (error) {
            console.error('期权渲染器初始化失败:', error);
        }
    }
    
    // 修改 loadData 函数，同时刷新期权数据
    async loadData(forceRefresh = false) {
        if (this.isRefreshing && !forceRefresh) return;
        
        this.isRefreshing = true;
        Renderer.showLoading(true);
        
        try {
            // 1. 检查后端数据版本并同步
            await this.syncWithBackend();
            
            // 2. 从 IndexedDB 获取持仓数据
            const positions = await this.db.getPositions();
            
            // 3. 分离不同类型的持仓
            const stockPositions = positions.filter(p => p.type !== 'cash_equivalent' && p.type !== 'option');
            const cashEquivalents = positions.filter(p => p.type === 'cash_equivalent');
            const options = positions.filter(p => p.type === 'option');
            
            // 4. 并行刷新股票和期权数据
            await Promise.all([
                // 刷新股票数据（现有逻辑）
                this.refreshStockData(stockPositions),
                
                // 刷新期权数据（新增）
                this.optionsRenderer?.refreshOptionsData(forceRefresh)
            ]);
            
            // 5. 计算和渲染
            // ... 现有计算逻辑
            
        } catch (error) {
            console.error('数据加载失败:', error);
        } finally {
            this.isRefreshing = false;
            Renderer.showLoading(false);
        }
    }
    
    // 手动刷新按钮事件
    bindEvents() {
        // 现有事件绑定...
        
        // 添加期权手动刷新按钮事件
        document.getElementById('refresh-options')?.addEventListener('click', () => {
            if (this.optionsRenderer) {
                this.optionsRenderer.triggerManualRefresh();
            }
        });
        
        // 全局刷新按钮也刷新期权数据
        document.getElementById('refresh-data')?.addEventListener('click', () => {
            this.loadData(true);
        });
    }
}
```

### 6.5 HTML表格结构
```html
<div class="section" id="options-section">
    <h2>期权持仓</h2>
    <div class="table-container">
        <table id="options-table" class="data-table">
            <thead>
                <tr>
                    <th>标的</th>
                    <th>期权代码</th>
                    <th>方向</th>
                    <th>合约数</th>
                    <th>行权价</th>
                    <th>开仓权利金</th>
                    <th>当前权利金</th>
                    <th>当前股价</th>
                    <th>价差(%)</th>
                    <th>虚实状态</th>
                    <th>距到期天数</th>
                    <th>盈亏平衡点</th>
                    <th>浮动盈亏</th>
                    <th>市值</th>
                </tr>
            </thead>
            <tbody id="options-table-body">
                <!-- 动态填充 -->
            </tbody>
        </table>
    </div>
</div>
```

## 7. 实施步骤

### 第一阶段：基础框架搭建（1-2天）
1. **创建期权工具模块**：`options-utils.js`
2. **创建期权渲染器**：`options-renderer.js`
3. **修改主页面**：添加期权表格HTML
4. **修改主程序**：集成期权模块

### 第二阶段：后端接口开发（2-3天）
1. **创建期权API路由**：`options.py`
2. **实现期权代码解析函数**
3. **实现批量查询接口**
4. **实现持仓详情接口**

### 第三阶段：前端集成测试（1-2天）
1. **集成期权渲染器**
2. **测试数据获取**
3. **测试表格渲染**
4. **测试自动刷新**

### 第四阶段：优化和调试（1-2天）
1. **性能优化**
2. **错误处理**
3. **样式优化**
4. **用户体验**

### 第五阶段：部署和监控（1天）
1. **部署到生产环境**
2. **监控API性能**
3. **收集用户反馈**

## 8. 关键技术点

### 8.1 期权代码解析
```python
def parse_option_symbol(option_symbol):
    """
    解析期权代码，支持美股和港股
    格式：标的代码 + 到期日(YYMMDD) + 期权类型(C/P) + 行权价
    示例：MU260424P358, TCH260430C370000, MU260424P357.5
    """
    # 支持小数和更长行权价的正则表达式
    pattern = r'^([A-Z]+)(\d{6})([CP])([\d\.]+)$'
    match = re.match(pattern, option_symbol)
    
    if match:
        symbol = match.group(1)  # 标的代码
        expiry = match.group(2)  # 到期日 YYMMDD
        option_type = 'call' if match.group(3) == 'C' else 'put'
        strike_price = float(match.group(4))  # 支持小数
        
        # 解析到期日
        year = int('20' + expiry[:2])
        month = int(expiry[2:4])
        day = int(expiry[4:6])
        expiry_date = f"{year:04d}-{month:02d}-{day:02d}"
        
        return {
            'symbol': symbol,
            'expiry_date': expiry_date,
            'option_type': option_type,
            'strike_price': strike_price
        }
    
    return None
```

### 8.2 yfinance API使用
```python
# 获取股票价格
ticker = yf.Ticker(symbol)
hist = ticker.history(period="1d")

# 获取期权链
options = ticker.option_chain(expiry_date)
option_data = options.puts[options.puts['strike'] == strike_price]
```

### 8.3 完整的盈亏分析函数
```javascript
// 完整的期权盈亏分析
static calculateProfitLossAnalysis(optionData, currentPrice, currentPremium) {
    const strikePrice = optionData.strike_price;
    const costPrice = optionData.cost_price;
    const optionType = optionData.option_type;
    const transactionType = optionData.transaction_type;
    const quantity = Math.abs(optionData.quantity);
    
    // 1. 盈亏平衡点
    // 重要：cost_price 总是正数，方向由 transaction_type 决定
    // - 卖出：收入权利金，cost_price 为正
    // - 买入：支出权利金，cost_price 为正
    let breakEven;
    if (optionType === 'put') {
        // 看跌期权盈亏平衡点
        if (transactionType === 'sell') {
            // 卖出看跌：行权价 - 权利金（收入降低盈亏平衡点）
            breakEven = strikePrice - costPrice;
        } else {
            // 买入看跌：行权价 - 权利金（支出提高盈亏平衡点）
            breakEven = strikePrice - costPrice;
        }
    } else {
        // 看涨期权盈亏平衡点
        if (transactionType === 'sell') {
            // 卖出看涨：行权价 + 权利金（收入提高盈亏平衡点）
            breakEven = strikePrice + costPrice;
        } else {
            // 买入看涨：行权价 + 权利金（支出降低盈亏平衡点）
            breakEven = strikePrice + costPrice;
        }
    }
    
    // 2. 最大盈利
    let maxProfit;
    if (transactionType === 'sell') {
        // 卖方最大盈利 = 收到的权利金
        maxProfit = costPrice * 100 * quantity;
    } else {
        // 买方最大盈利理论上无限（看涨）或行权价-权利金（看跌）
        if (optionType === 'call') {
            maxProfit = '无限';
        } else {
            maxProfit = (strikePrice - costPrice) * 100 * quantity;
        }
    }
    
    // 3. 最大亏损
    let maxLoss;
    if (transactionType === 'sell') {
        // 卖方最大亏损理论上无限（看涨）或行权价-权利金（看跌）
        if (optionType === 'call') {
            maxLoss = '无限';
        } else {
            maxLoss = (strikePrice - costPrice) * 100 * quantity;
        }
    } else {
        // 买方最大亏损 = 支付的权利金
        maxLoss = costPrice * 100 * quantity;
    }
    
    // 4. 当前盈亏状态
    let currentStatus;
    if (optionType === 'put') {
        if (transactionType === 'sell') {
            // 卖出看跌：股价高于盈亏平衡点为盈利
            currentStatus = currentPrice > breakEven ? '盈利' : '亏损';
        } else {
            // 买入看跌：股价低于盈亏平衡点为盈利
            currentStatus = currentPrice < breakEven ? '盈利' : '亏损';
        }
    } else {
        if (transactionType === 'sell') {
            // 卖出看涨：股价低于盈亏平衡点为盈利
            currentStatus = currentPrice < breakEven ? '盈利' : '亏损';
        } else {
            // 买入看涨：股价高于盈亏平衡点为盈利
            currentStatus = currentPrice > breakEven ? '盈利' : '亏损';
        }
    }
    
    // 5. 安全边际（针对卖方策略）
    let safetyMargin = null;
    if (transactionType === 'sell') {
        if (optionType === 'put') {
            // 卖出看跌的安全边际 = (当前股价 - 盈亏平衡点) / 当前股价
            safetyMargin = ((currentPrice - breakEven) / currentPrice * 100).toFixed(2);
        } else {
            // 卖出看涨的安全边际 = (盈亏平衡点 - 当前股价) / 当前股价
            safetyMargin = ((breakEven - currentPrice) / currentPrice * 100).toFixed(2);
        }
    }
    
    return {
        盈亏平衡点: breakEven.toFixed(2),
        最大盈利: typeof maxProfit === 'string' ? maxProfit : maxProfit.toFixed(2),
        最大亏损: typeof maxLoss === 'string' ? maxLoss : maxLoss.toFixed(2),
        当前盈亏状态: currentStatus,
        安全边际: safetyMargin ? safetyMargin + '%' : null
    };
}

### 8.4 合约乘数获取函数
```javascript
// 获取合约乘数（数据库中已有 contract_multiplier 字段）
static getContractMultiplier(optionData) {
    // 直接使用数据库中的 contract_multiplier 字段
    // 该字段位于 option_details 对象中
    return optionData.contract_multiplier || 100;
}

// 数据提取函数（包含合约乘数）
static extractOptionData(position) {
    if (position.type !== 'option' || !position.option_details) {
        return null;
    }
    
    const details = position.option_details;
    return {
        // 基础信息
        symbol: position.symbol,
        option_symbol: details.option_symbol,
        quantity: position.quantity,  // 带符号：正数为买入，负数为卖出
        
        // 权利金（重要：cost_price 总是正数）
        // - 卖出：收入权利金，cost_price 为正
        // - 买入：支出权利金，cost_price 为正
        // 方向由 transaction_type 字段决定
        cost_price: position.cost_price,
        
        // 期权详情
        strike_price: details.strike_price,
        expiration_date: details.expiry_date || details.expiration_date,
        option_type: details.option_type,           // 'call' 或 'put'
        transaction_type: details.transaction_type, // 'buy' 或 'sell'
        
        // 合约乘数（数据库中已有）
        contract_multiplier: details.contract_multiplier || 100,
        
        // 其他分析字段
        intrinsic_value: details.intrinsic_value,
        time_value: details.time_value,
        implied_volatility: details.implied_volatility
    };
}

### 8.5 修正后的浮动盈亏计算
```javascript
// 修正后的浮动盈亏计算（考虑合约乘数和交易方向）
static calculateFloatingPnl(optionData, currentPremium) {
    const costPrice = optionData.cost_price;
    const quantity = Math.abs(optionData.quantity);
    const transactionType = optionData.transaction_type; // 'buy' 或 'sell'
    
    // 获取合约乘数
    const multiplier = this.getContractMultiplier(optionData.symbol, optionData);
    
    // 计算每股盈亏（考虑交易方向）
    let pnlPerShare;
    if (transactionType === 'sell') {
        // 卖方：权利金下降为盈利，上升为亏损
        pnlPerShare = (costPrice - currentPremium) * multiplier;
    } else {
        // 买方：权利金上升为盈利，下降为亏损
        pnlPerShare = (currentPremium - costPrice) * multiplier;
    }
    
    // 总盈亏
    const totalPnl = pnlPerShare * quantity;
    
    return {
        每股盈亏: pnlPerShare.toFixed(2),
        总盈亏: totalPnl.toFixed(2),
        合约乘数: multiplier,
        交易方向: transactionType === 'sell' ? '卖方' : '买方',
        盈亏状态: totalPnl > 0 ? '盈利' : totalPnl < 0 ? '亏损' : '持平'
    };
}

// 修正后的市值计算
static calculateMarketValue(optionData, currentPremium) {
    const quantity = Math.abs(optionData.quantity);
    const multiplier = this.getContractMultiplier(optionData.symbol, optionData);
    
    // 市值 = 当前权利金 × 合约乘数 × 合约数
    const marketValue = currentPremium * multiplier * quantity;
    
    return marketValue;
}
```

### 8.6 安全边际与颜色映射函数（核心实现）
```javascript
// 计算安全边际（价差百分比）
static calculateSafetyMargin(optionData, currentPrice) {
    const strikePrice = optionData.strike_price;
    const optionType = optionData.option_type; // 'call' 或 'put'
    
    // 计算价差百分比
    let priceDiffPercent;
    if (optionType === 'put') {
        // 看跌期权：安全边际 = (当前股价 - 行权价) / 当前股价
        priceDiffPercent = ((currentPrice - strikePrice) / currentPrice) * 100;
    } else {
        // 看涨期权：安全边际 = (行权价 - 当前股价) / 当前股价
        priceDiffPercent = ((strikePrice - currentPrice) / currentPrice) * 100;
    }
    
    return priceDiffPercent;
}

// 根据安全边际获取虚实状态和颜色
static getMoneynessAndColor(optionData, currentPrice) {
    const safetyMargin = this.calculateSafetyMargin(optionData, currentPrice);
    
    // 判断实值/虚值（仅用于文字显示）
    let moneynessText;
    if (optionData.option_type === 'call') {
        moneynessText = currentPrice > optionData.strike_price ? '实值' : '虚值';
    } else {
        moneynessText = currentPrice < optionData.strike_price ? '实值' : '虚值';
    }
    
    // 根据安全边际确定颜色（核心逻辑）
    let color;
    let status;
    
    if (moneynessText === '实值') {
        // 实值：一律红色（危险）
        color = 'red';
        status = '危险';
    } else {
        // 虚值：根据安全边际分级
        if (safetyMargin > 10) {
            color = 'green';
            status = '安全';
        } else if (safetyMargin > 5) {
            color = 'blue';
            status = '关注';
        } else if (safetyMargin > 0) {
            color = 'yellow';
            status = '警示';
        } else {
            // 价差为0或负值（平值或实值）
            color = 'red';
            status = '危险';
        }
    }
    
    return {
        安全边际: safetyMargin.toFixed(2) + '%',
        虚实状态: moneynessText,
        状态颜色: color,
        风险等级: status,
        价差百分比: safetyMargin.toFixed(2)
    };
}

// 完整的期权状态分析
static analyzeOptionStatus(optionData, currentPrice, currentPremium) {
    const safetyMargin = this.calculateSafetyMargin(optionData, currentPrice);
    const moneynessInfo = this.getMoneynessAndColor(optionData, currentPrice);
    const pnlAnalysis = this.calculateProfitLossAnalysis(optionData, currentPrice, currentPremium);
    
    return {
        // 安全边际分析
        ...moneynessInfo,
        
        // 盈亏分析
        ...pnlAnalysis,
        
        // 综合风险评估
        综合风险: safetyMargin > 10 ? '低风险' : 
                 safetyMargin > 5 ? '中风险' : 
                 safetyMargin > 0 ? '高风险' : '极高风险',
        
        // 决策建议（针对卖出看跌期权）
        决策建议: optionData.transaction_type === 'sell' && optionData.option_type === 'put' ? 
            safetyMargin > 10 ? '持有' :
            safetyMargin > 5 ? '密切关注' :
            safetyMargin > 0 ? '考虑平仓' : '立即平仓'
            : '根据策略调整'
    };
}
```

### 8.7 实施标准说明
**重要**：所有期权状态和盈亏计算必须使用以下函数：

#### 8.7.1 必须使用的核心函数
1. **安全边际与颜色**：使用 `getMoneynessAndColor()` 函数
2. **浮动盈亏计算**：使用 `calculateFloatingPnl()` 函数
3. **市值计算**：使用 `calculateMarketValue()` 函数
4. **完整分析**：使用 `analyzeOptionStatus()` 函数

#### 8.7.2 颜色映射原则（严禁违反）
```javascript
// ❌ 绝对禁止：使用 is_itm 判断颜色
if (is_itm) {
    color = 'red';  // 错误：实值不一定危险（浅度实值）
} else {
    color = 'green'; // 错误：虚值不一定安全（浅度虚值）
}

// ✅ 必须使用：基于安全边际的颜色映射
const statusInfo = OptionsUtils.getMoneynessAndColor(optionData, currentPrice);
const color = statusInfo.状态颜色;  // 基于安全边际百分比
```

#### 8.7.3 完整实现示例
```javascript
// ✅ 正确：完整的状态分析
const optionData = OptionsUtils.extractOptionData(position);
const statusAnalysis = OptionsUtils.analyzeOptionStatus(
    optionData, 
    currentPrice, 
    currentPremium
);

// 表格渲染使用
const rowColor = statusAnalysis.状态颜色;          // 基于安全边际
const moneynessText = statusAnalysis.虚实状态;     // 实值/虚值文字
const safetyMargin = statusAnalysis.安全边际;      // 价差百分比
const riskLevel = statusAnalysis.风险等级;         // 安全/关注/警示/危险
```

#### 8.7.4 错误示例（严禁使用）
```javascript
// ❌ 错误：基于 is_itm 的颜色判断
const is_itm = currentPrice > strikePrice;  // 仅看涨期权
const color = is_itm ? 'red' : 'green';     // 严重误导！

// ❌ 错误：硬编码计算
const pnl = (costPrice - currentPremium) * 100 * quantity;

// ❌ 错误：忽略交易方向
const pnl = Math.abs(costPrice - currentPremium) * 100 * quantity;
```

## 9. 风险与挑战

### 9.1 技术风险
- **yfinance API限制**：频率限制和稳定性问题
- **网络延迟**：批量查询可能较慢
- **数据一致性**：股票和期权价格时间戳同步

### 9.2 应对策略
- **缓存机制**：减少API调用频率
- **错误重试**：网络错误自动重试
- **降级方案**：API不可用时显示缓存数据

## 10. 成功标准

### 10.1 功能标准
- [ ] 正确显示所有期权持仓
- [ ] 实时更新价格数据
- [ ] 准确计算分析指标
- [ ] 视觉状态标识清晰

### 10.2 性能标准
- [ ] 页面加载时间 < 3秒
- [ ] 数据刷新间隔 ≤ 60秒
- [ ] API响应时间 < 2秒

### 10.3 用户体验标准
- [ ] 表格信息清晰易懂
- [ ] 颜色标识直观有效
- [ ] 操作响应及时

## 11. 附录

### 11.1 文件清单
1. `portfolio-v5/js/options-utils.js`
2. `portfolio-v5/js/options-renderer.js`
3. `api/portfolio/options.py`
4. `portfolio-v5/index.html`（修改部分）

### 11.2 依赖库
- 前端：原生JavaScript，无额外依赖
- 后端：yfinance, FastAPI, pandas

### 11.3 测试数据
```javascript
// 测试用期权持仓数据
const testOptionPosition = {
    symbol: "MU",
    type: "option",
    quantity: -1,
    cost_price: 2.5,
    option_details: {
        option_symbol: "MU260424P358",
        strike_price: 358,
        expiration_date: "2026-04-24",
        option_type: "put",
        transaction_type: "sell"
    }
};
```

---

**方案总结**：本方案基于现有数据库结构，通过前端计算和服务层API实现完整的期权持仓展示功能，无需修改数据库，实施风险低，效果显著。

**文档创建时间**：2026-04-17 22:10
**文档位置**：`/Users/donghao/.openclaw/workspace-bulma/portfolio-v5/docs/期权持仓展示模块优化方案.md`